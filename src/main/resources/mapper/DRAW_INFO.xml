<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="draw.repository.DrawRepo">
    <select id="SELECT_DRAW" resultType="draw.domain.DrawDomain">
        SELECT * FROM g_draw_list
    </select>

    <insert id="INSERT_DRAW" parameterType="draw.domain.DrawDomain">
        INSERT INTO g_draw_list (file, title, reg_dt) VALUES (#{file}, #{title},  #{reg_dt, jdbcType=TIMESTAMP})

    </insert>

    <select id="SELECT_DRAW_BY_ID" parameterType="draw.domain.DrawDomain" >
        SELECT * FROM draw_table WHERE id = #{seq}
    </select>

</mapper>
