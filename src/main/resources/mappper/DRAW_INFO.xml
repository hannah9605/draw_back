<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="repository.LAYERRepo">

    <select id="LIST_DRAW" parameterType="LayerDomain" resultType="DrawDomain">
        <include refid="Function.chkNotEmpty" />
        SELECT *
        FROM g_draw_list
        WHERE 1 = 1

    </select>


</mapper>